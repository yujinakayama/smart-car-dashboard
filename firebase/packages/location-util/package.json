{
  "name": "@dash/location-util",
  "version": "1.0.0",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "private": true,
  "scripts": {
    "clean": "rm -rf lib tsconfig.tsbuildinfo",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write . && eslint --fix .",
    "build": "tsc --build"
  }
}

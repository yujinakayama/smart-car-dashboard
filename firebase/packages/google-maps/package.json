{
  "name": "@dash/google-maps",
  "version": "1.0.0",
  "main": "lib/index.js",
  "private": true,
  "scripts": {
    "clean": "rm -rf lib tsconfig.tsbuildinfo",
    "lint": "prettier --check . && eslint .",
    "format": "prettier --write . && eslint --fix .",
    "build": "tsc --build"
  },
  "dependencies": {
    "@dash/text-util": "file:../text-util"
  },
  "devDependencies": {
    "@googlemaps/google-maps-services-js": "^3.3.42"
  }
}
